# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Yarn (node-modules linker) — keep yarn.lock, ignore local state
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnp.*
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Brainstorming visual companion
.superpowers/

# Vendored agent skills. `skills-lock.json` is the manifest — restore with
# `npx skills add`, the same way node_modules is restored from a lockfile.
.agents/
.claude/

# Vercel CLI link state
.vercel
.env*
